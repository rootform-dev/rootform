{
  "format_version": 1,
  "commands": [
    {
      "path": "rootform",
      "usage": "rootform [flags]",
      "summary": "Understand Terraform and OpenTofu as architecture",
      "description": "Rootform turns Terraform and OpenTofu into a semantic, deterministic,\nand explainable architecture.\n\nStart with rootform run ./infra to explore a directory locally.",
      "examples": "rootform run ./infra\n  rootform build ./infra -o architecture.json\n  rootform check ./infra\n  rootform diff ./before ./after",
      "subcommands": [
        "rootform build",
        "rootform check",
        "rootform compile",
        "rootform completion",
        "rootform diff",
        "rootform explain",
        "rootform fmt",
        "rootform init",
        "rootform list",
        "rootform lsp",
        "rootform package",
        "rootform publish",
        "rootform run",
        "rootform show",
        "rootform test",
        "rootform validate",
        "rootform vendor",
        "rootform version"
      ],
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform",
          "no_option_default": "true"
        },
        {
          "name": "version",
          "shorthand": "v",
          "type": "bool",
          "default": "false",
          "usage": "print the rootform version and exit",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform build",
      "usage": "rootform build [directory] [flags]",
      "summary": "Build an architecture",
      "description": "Build a semantic architecture from Terraform or OpenTofu configuration\nor from a plan in JSON format.\n\nWith no directory, build reads the current directory. --plan reads a plan\ninstead; use - to read it from standard input. Directory input never\ndownloads, acquires, or prompts: selected external content must already\nbe available locally, or be prepared explicitly with rootform init or\nrootform vendor. A coherent local lock is silent.\n\nThe Rootform architecture file is written to standard output, or to the\nfile selected by --output. With --format html the architecture is written\nas one page that opens on its own, with no server and no other file.\nDiagnostics go to standard error.\n\nExit status:\n  0  the architecture was built\n  2  the command was used incorrectly\n  3  no complete architecture could be built",
      "examples": "rootform build\n  rootform build ./infra\n  rootform build ./infra -o architecture.json\n  rootform build ./infra --format html -o architecture.html\n  rootform build --plan tfplan.json",
      "flags": [
        {
          "name": "format",
          "type": "string",
          "default": "json",
          "usage": "write `format` (json/html)"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform build",
          "no_option_default": "true"
        },
        {
          "name": "locked",
          "type": "bool",
          "default": "false",
          "usage": "require an existing valid rootform.lock",
          "no_option_default": "true"
        },
        {
          "name": "output",
          "shorthand": "o",
          "type": "string",
          "default": "",
          "usage": "write the architecture to this `file`"
        },
        {
          "name": "plan",
          "type": "file",
          "default": "",
          "usage": "read JSON plan; - reads standard input"
        }
      ]
    },
    {
      "path": "rootform check",
      "usage": "rootform check [input] [flags]",
      "summary": "Check architecture policies",
      "description": "Build or load an architecture and evaluate the applicable policies.\n\nThe input can be an infrastructure directory, a Rootform architecture\nfile, or - for an architecture file on standard input. With no input,\ncheck reads the current directory. --plan reads a plan in JSON format\ninstead. Directory input never downloads, acquires, or prompts: selected\nexternal content must already be available locally, or be prepared\nexplicitly with rootform init or rootform vendor. A coherent local lock\nis silent. An explicit --policy-pack selection replaces the project's\nPolicy Pack selection for this invocation.\n\nThe selected text, JSON, SARIF, or Markdown result goes to standard\noutput, or to --output. Diagnostics go to standard error.\n\nExit status:\n  0  all selected policies were evaluated and compliant\n  1  at least one policy was violated, including in mixed runs\n  2  the command was used incorrectly\n  3  indeterminate or not_evaluated, with no confirmed violation\n\nViolations take precedence: exit 1. Zero policies or zero evaluations\nare never compliant. A selected policy without targets prevents compliance.\n--policy-pack accepts a source directory or compiled JSON file; compiled\nfiles retain their semantic pins during evaluation.",
      "examples": "rootform check\n  rootform check ./infra\n  rootform check . --locked\n  rootform check --plan tfplan.json\n  rootform check . --policy-pack ./policies\n  rootform check arch.json --policy-pack pack.json\n  rootform build ./infra | rootform check -\n  rootform check ./infra --format sarif -o rootform.sarif",
      "flags": [
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "text/json/sarif/markdown `format`"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform check",
          "no_option_default": "true"
        },
        {
          "name": "locked",
          "type": "bool",
          "default": "false",
          "usage": "require an existing valid rootform.lock",
          "no_option_default": "true"
        },
        {
          "name": "output",
          "shorthand": "o",
          "type": "string",
          "default": "",
          "usage": "write the result to this `file`"
        },
        {
          "name": "plan",
          "type": "file",
          "default": "",
          "usage": "read JSON plan; - reads standard input"
        },
        {
          "name": "policy",
          "type": "stringArray",
          "default": "[]",
          "usage": "select pack/name or unique policy; repeatable"
        },
        {
          "name": "policy-pack",
          "type": "stringArray",
          "default": "[]",
          "usage": "select directory or compiled JSON `path`; repeatable"
        }
      ]
    },
    {
      "path": "rootform compile",
      "usage": "rootform compile \u003cobject\u003e [flags]",
      "summary": "Compile a Policy Pack for offline checks",
      "description": "Compile Policy Pack sources with explicit semantic pins for later checks.",
      "subcommands": [
        "rootform compile policy-pack"
      ],
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform compile",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform compile policy-pack",
      "usage": "rootform compile policy-pack \u003cdirectory\u003e [flags]",
      "summary": "Compile and pin a Policy Pack",
      "description": "Compile one Policy Pack source directory against the semantic snapshot\nin --semantics. Write the compiled JSON file to --output, preserving its\nsemantic pins for later offline checks without producer Dialects.\nSummary goes to standard output. Diagnostics go to standard error.\n\nExit status:\n  0  the compiled Policy Pack was written\n  1  the Policy Pack could not be compiled or written\n  2  the command was used incorrectly",
      "examples": "rootform compile policy-pack ./policies --semantics arch.json -o pack.json\n  rootform compile policy-pack . --semantics baseline.json -o pack.json\n  rootform compile policy-pack ./rules --semantics arch.json -o rules.json",
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform compile policy-pack",
          "no_option_default": "true"
        },
        {
          "name": "output",
          "shorthand": "o",
          "type": "string",
          "default": "",
          "usage": "write compiled Policy Pack to `file` (required)"
        },
        {
          "name": "semantics",
          "type": "string",
          "default": "",
          "usage": "pin semantics from an architecture `file` (required)"
        }
      ]
    },
    {
      "path": "rootform completion",
      "usage": "rootform completion \u003cshell\u003e [flags]",
      "summary": "Generate shell completion",
      "description": "Generate a completion script for bash, zsh, fish, or PowerShell.\n\nThe script goes to standard output. Diagnostics go to standard error.\n\nExit status:\n  0  the completion script was generated\n  1  the completion script could not be generated\n  2  the command was used incorrectly",
      "examples": "rootform completion bash \u003e /usr/local/etc/bash_completion.d/rootform\n  rootform completion zsh \u003e \"${fpath[1]}/_rootform\"\n  rootform completion fish \u003e ~/.config/fish/completions/rootform.fish",
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform completion",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform diff",
      "usage": "rootform diff \u003cbefore\u003e \u003cafter\u003e [flags]",
      "summary": "Compare two architectures",
      "description": "Compare two architectures and report the architectural changes between\nthem.\n\nEach input can be an infrastructure directory, a Rootform architecture\nfile, or - for one architecture file on standard input. --plan reads\nboth sides from a plan in JSON format instead. Only one input can be -.\n\nThe selected text, JSON, or Markdown result goes to standard output, or\nto --output. Diagnostics go to standard error.\n\nExit status:\n  0  the comparison succeeded\n  1  changes or undetermined facts with --exit-code\n  2  the command was used incorrectly\n  3  the comparison could not be completed",
      "examples": "rootform diff ./before ./after\n  rootform diff before.json after.json --exit-code\n  rootform diff --plan tfplan.json\n  terraform show -json tfplan | rootform diff --plan -\n  rootform diff ./before ./after --format markdown -o rootform-diff.md",
      "flags": [
        {
          "name": "exit-code",
          "type": "bool",
          "default": "false",
          "usage": "exit 1 for changes or undetermined facts",
          "no_option_default": "true"
        },
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text, json, or markdown"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform diff",
          "no_option_default": "true"
        },
        {
          "name": "output",
          "shorthand": "o",
          "type": "string",
          "default": "",
          "usage": "write the diff to this `file`"
        },
        {
          "name": "plan",
          "type": "file",
          "default": "",
          "usage": "read JSON plan; - reads standard input"
        }
      ]
    },
    {
      "path": "rootform explain",
      "usage": "rootform explain \u003cobject\u003e \u003cname\u003e [flags]",
      "summary": "Explain an architecture result",
      "description": "Explain how an architecture element was derived, how a source\ndeclaration was interpreted, or why a policy produced a result.",
      "examples": "rootform explain architecture google_sql_database_instance.main\n  rootform explain semantics google.rule.cloud-sql-instance\n  rootform explain policy baseline.policy.cluster-network-context",
      "subcommands": [
        "rootform explain architecture",
        "rootform explain policy",
        "rootform explain semantics"
      ],
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform explain",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform explain architecture",
      "usage": "rootform explain architecture \u003caddress\u003e [flags]",
      "summary": "Explain an architecture element",
      "description": "Show how an architecture element is implemented and which semantic\nfacts and source locations support it.\n\nBy default, explain reads the current directory. --input accepts an\ninfrastructure directory, a Rootform architecture file, or - for an\narchitecture file on standard input.\n\nThe text or JSON explanation goes to standard output. Diagnostics go to\nstandard error.\n\nExit status:\n  0  the element was explained\n  1  the declaration or architecture element was not found\n  2  the command was used incorrectly\n  3  no explanation could be decided",
      "examples": "rootform explain architecture google_compute_network.vpc\n  rootform explain architecture kubernetes_deployment_v1.app --format json\n  rootform explain architecture google_compute_network.vpc --input prod.json",
      "flags": [
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform explain architecture",
          "no_option_default": "true"
        },
        {
          "name": "input",
          "type": "string",
          "default": "",
          "usage": "read the architecture at `path`; - reads standard input"
        }
      ]
    },
    {
      "path": "rootform explain policy",
      "usage": "rootform explain policy \u003cidentifier\u003e [flags]",
      "summary": "Explain a policy result",
      "description": "Show why a policy passed, failed, or could not be evaluated for an\narchitecture element.\n\nThe current directory supplies the architecture.\nThe project must select the Policy Pack that owns the policy.\nUse \u003cpolicy-pack\u003e.policy.\u003cname\u003e, or a bare name when it resolves unambiguously.\n\nThe text or JSON explanation goes to standard output. Diagnostics go to\nstandard error.\n\nExit status:\n  0  the result was explained\n  1  the named definition was not found\n  2  the command was used incorrectly\n  3  no explanation could be decided",
      "examples": "rootform explain policy baseline.policy.cluster-network-context\n  rootform explain policy cluster-network-context\n  rootform explain policy baseline.policy.cluster-network-context --format json",
      "flags": [
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform explain policy",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform explain semantics",
      "usage": "rootform explain semantics \u003cidentifier\u003e [flags]",
      "summary": "Explain a semantic interpretation",
      "description": "Show how a source declaration was interpreted, including the matching\nrule and the architecture it produced.\n\nThe current directory supplies the architecture.\nUse \u003cowner\u003e.\u003ckind\u003e.\u003cname\u003e, or a bare name when it resolves unambiguously.\n\nThe text or JSON explanation goes to standard output. Diagnostics go to\nstandard error.\n\nExit status:\n  0  the result was explained\n  1  the named definition was not found\n  2  the command was used incorrectly\n  3  no explanation could be decided",
      "examples": "rootform explain semantics google.rule.cloud-sql-instance\n  rootform explain semantics cloud-sql-instance\n  rootform explain semantics google.rule.cloud-sql-instance --format json",
      "flags": [
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform explain semantics",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform fmt",
      "usage": "rootform fmt [path] [flags]",
      "summary": "Format Rootform files",
      "description": "Rewrite Rootform source files using the canonical format.\n\nWith no path, fmt reads the current directory. By default it rewrites\nchanged files. --check reports their names and --diff writes changes to\nstandard output without rewriting. Diagnostics go to standard error.\n\nExit status:\n  0  the sources are formatted\n  1  --check or --diff found a source that is not formatted\n  2  the command was used incorrectly\n  3  a source could not be read, parsed, or rewritten",
      "examples": "rootform fmt\n  rootform fmt ./dialects\n  rootform fmt --check\n  rootform fmt ./dialects --diff",
      "flags": [
        {
          "name": "check",
          "type": "bool",
          "default": "false",
          "usage": "check formatting without rewriting files",
          "no_option_default": "true"
        },
        {
          "name": "diff",
          "type": "bool",
          "default": "false",
          "usage": "show formatting changes without rewriting files",
          "no_option_default": "true"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform fmt",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform init",
      "usage": "rootform init [path] [flags]",
      "summary": "Prepare a Rootform project",
      "description": "Prepare an existing rootform.lock and materialize the project's exact\nnon-embedded selections (dialects and Policy Pack sources) locally.\n\ninit is explicit: it never detects providers, selects another version,\nor writes rootform.lock. With --locked the existing rootform.lock is\nrequired and valid; without it, a missing lock is an empty selection\nand an existing lock is always preserved. Supplied dialects ship\ninside the release set and are never acquired. When network access is\navailable and not disabled by --offline, init fetches only the exact\nmanifest digests already pinned by rootform.lock.\n\nPath defaults to . and is both the project and Terraform or OpenTofu\nroot. Machine JSON goes to standard output. Diagnostics and verbose\ndetail go to standard error.\n\nExit status:\n  0  preparation completed\n  1  preparation failed\n  2  the command was used incorrectly\n  3  no deterministic preparation was available",
      "examples": "rootform init\n  rootform init ./infra\n  rootform init ./infra --locked --offline\n  rootform init ./infra --no-input\n  rootform init ./infra --format json",
      "flags": [
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform init",
          "no_option_default": "true"
        },
        {
          "name": "locked",
          "type": "bool",
          "default": "false",
          "usage": "require and preserve the existing rootform.lock",
          "no_option_default": "true"
        },
        {
          "name": "no-input",
          "type": "bool",
          "default": "false",
          "usage": "never prompt; require deterministic action",
          "no_option_default": "true"
        },
        {
          "name": "offline",
          "type": "bool",
          "default": "false",
          "usage": "disable network; use only local data",
          "no_option_default": "true"
        },
        {
          "name": "verbose",
          "shorthand": "v",
          "type": "bool",
          "default": "false",
          "usage": "show provider evidence and origin",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform list",
      "usage": "rootform list \u003cobject\u003e [flags]",
      "summary": "List Rootform definitions",
      "description": "List the dialects a project loads, the Policy Packs it selects, or\nthe policies those Policy Packs declare.\n\nUse \"rootform show\" to read one listed definition in full.",
      "examples": "rootform list dialects\n  rootform list dialects -o wide\n  rootform list policy-packs\n  rootform list policies",
      "subcommands": [
        "rootform list dialects",
        "rootform list policies",
        "rootform list policy-packs"
      ],
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform list",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform list dialects",
      "usage": "rootform list dialects [flags]",
      "summary": "List the dialect catalog",
      "description": "List the dialect catalog available to this project: the dialects\nsupplied with rootform and the dialects the project selects.\n\nWith no --dialect selection, every loaded dialect is included. The\ndefault listing names one dialect per line, -o wide adds version,\norigin and how much each dialect declares, and -o json carries the\nexact version, origin and content digest of every selection.\n\nThe listing goes to standard output. Diagnostics go to standard\nerror.\n\nExit status:\n  0  the definitions were listed\n  2  the command was used incorrectly\n  3  the selected definitions could not be read",
      "examples": "rootform list dialects\n  rootform list dialects -o wide\n  rootform list dialects --dialect google\n  rootform list dialects -o json",
      "flags": [
        {
          "name": "dialect",
          "type": "stringArray",
          "default": "[]",
          "usage": "limit results to this dialect `name`; repeatable"
        },
        {
          "name": "format",
          "shorthand": "o",
          "type": "string",
          "default": "",
          "usage": "output `format`: text, wide, or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform list dialects",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform list policies",
      "usage": "rootform list policies [flags]",
      "summary": "List policies",
      "description": "List the policies the selected Policy Packs declare.\n\nWith --policy-pack, only the supplied local authoring roots are\nread; otherwise the project selection is loaded. The default\nlisting names one qualified policy per line, -o wide adds what\neach policy targets, and -o json carries the owning Policy Pack\nand target of every policy.\n\nThe listing goes to standard output. Diagnostics go to standard\nerror.\n\nExit status:\n  0  the definitions were listed\n  2  the command was used incorrectly\n  3  the selected definitions could not be read",
      "examples": "rootform list policies\n  rootform list policies -o wide\n  rootform list policies --policy-pack ./policies\n  rootform list policies -o json",
      "flags": [
        {
          "name": "format",
          "shorthand": "o",
          "type": "string",
          "default": "",
          "usage": "output `format`: text, wide, or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform list policies",
          "no_option_default": "true"
        },
        {
          "name": "policy-pack",
          "type": "stringArray",
          "default": "[]",
          "usage": "select local Policy Pack `directory`; repeatable"
        }
      ]
    },
    {
      "path": "rootform list policy-packs",
      "usage": "rootform list policy-packs [flags]",
      "summary": "List Policy Packs",
      "description": "List the Policy Packs the project selects or that are supplied\nlocally.\n\nWith --policy-pack, only the supplied local authoring roots are\nread; otherwise the project selection is loaded. The default\nlisting names one Policy Pack per line, -o wide adds version and\nhow many policies each declares, and -o json carries the exact\nversion and content digest.\n\nThe listing goes to standard output. Diagnostics go to standard\nerror.\n\nExit status:\n  0  the definitions were listed\n  2  the command was used incorrectly\n  3  the selected definitions could not be read",
      "examples": "rootform list policy-packs\n  rootform list policy-packs -o wide\n  rootform list policy-packs --policy-pack ./policies\n  rootform list policy-packs -o json",
      "flags": [
        {
          "name": "format",
          "shorthand": "o",
          "type": "string",
          "default": "",
          "usage": "output `format`: text, wide, or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform list policy-packs",
          "no_option_default": "true"
        },
        {
          "name": "policy-pack",
          "type": "stringArray",
          "default": "[]",
          "usage": "select local Policy Pack `directory`; repeatable"
        }
      ]
    },
    {
      "path": "rootform lsp",
      "usage": "rootform lsp [flags]",
      "summary": "Serve Rootform language features over stdio",
      "description": "Run the Rootform language server over standard input\nand standard output. Protocol frames are the only standard output. Process\ndiagnostics go to standard error; source diagnostics travel through LSP.\n\nExit status:\n  0  the client completed shutdown and exit\n  1  transport or lifecycle failed\n  2  the command was used incorrectly",
      "examples": "rootform lsp\n  rootform lsp 2\u003erootform-lsp.log\n  rootform lsp \u003cclient.frames \u003eserver.frames",
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform lsp",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform package",
      "usage": "rootform package \u003cobject\u003e [flags]",
      "summary": "Package Rootform content for distribution",
      "description": "Build deterministic local registry layouts from validated Rootform packages.",
      "examples": "rootform package dialects ./dialects --to ./artifacts/dialects\n  rootform package policy-packs ./policies --to ./artifacts/policies",
      "subcommands": [
        "rootform package dialects",
        "rootform package policy-packs"
      ],
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform package",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform package dialects",
      "usage": "rootform package dialects \u003cdirectory\u003e [flags]",
      "summary": "Build dialect packages",
      "description": "Compile an external dialect source set and write one deterministic,\nlocal-only registry layout of exact dialect packages. Supplied\ndialects stay embedded and are never packaged. Summary goes to\nstandard output. Diagnostics go to standard error. Nothing is sent\nto a registry.\n\nExit status:\n  0  registry layout was written\n  1  dialects could not be packaged\n  2  the command was used incorrectly",
      "examples": "rootform package dialects ./dialects --to ./artifacts/oci\n  rootform package dialects . --to ./artifacts/oci\n  rootform package dialects ./own --to ./oci --licenses MPL-2.0",
      "flags": [
        {
          "name": "documentation-url",
          "type": "string",
          "default": "",
          "usage": "record documentation `url` in provenance"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform package dialects",
          "no_option_default": "true"
        },
        {
          "name": "licenses",
          "type": "string",
          "default": "",
          "usage": "record SPDX license `expression` in OCI provenance"
        },
        {
          "name": "revision",
          "type": "string",
          "default": "",
          "usage": "record source-control `revision` in OCI provenance"
        },
        {
          "name": "source-url",
          "type": "string",
          "default": "",
          "usage": "record canonical source `url` in OCI provenance"
        },
        {
          "name": "to",
          "type": "string",
          "default": "",
          "usage": "write registry layout to `directory`"
        }
      ]
    },
    {
      "path": "rootform package policy-packs",
      "usage": "rootform package policy-packs \u003cdirectory\u003e [flags]",
      "summary": "Build Policy Pack packages",
      "description": "Compile a Policy Pack source set and write one deterministic local registry\nlayout containing exact Policy Pack packages.\nNothing is sent to a registry. Summary goes to standard output.\nDiagnostics go to standard error.\n\nExit status:\n  0  registry layout was written\n  1  Policy Packs could not be packaged\n  2  the command was used incorrectly",
      "examples": "rootform package policy-packs ./policies --to ./artifacts/policies\n  rootform package policy-packs ./baseline --to ./baseline-oci\n  rootform package policy-packs . --to ./oci",
      "flags": [
        {
          "name": "documentation-url",
          "type": "string",
          "default": "",
          "usage": "record documentation `url` in provenance"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform package policy-packs",
          "no_option_default": "true"
        },
        {
          "name": "licenses",
          "type": "string",
          "default": "",
          "usage": "record SPDX license `expression` in OCI provenance"
        },
        {
          "name": "revision",
          "type": "string",
          "default": "",
          "usage": "record source-control `revision` in OCI provenance"
        },
        {
          "name": "source-url",
          "type": "string",
          "default": "",
          "usage": "record canonical source `url` in OCI provenance"
        },
        {
          "name": "to",
          "type": "string",
          "default": "",
          "usage": "write registry layout to `directory`"
        }
      ]
    },
    {
      "path": "rootform publish",
      "usage": "rootform publish \u003cobject\u003e [flags]",
      "summary": "Publish packaged Rootform content",
      "description": "Publish validated Rootform packages to a registry repository.",
      "examples": "rootform publish dialects ./oci --to r.example/acme/dialects\n  rootform publish policy-packs ./oci --to r.example/acme/policies",
      "subcommands": [
        "rootform publish dialects",
        "rootform publish policy-packs"
      ],
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform publish",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform publish dialects",
      "usage": "rootform publish dialects \u003clayout\u003e [flags]",
      "summary": "Publish a verified dialect registry layout",
      "description": "Validate an existing local Rootform registry layout, publish its external\nDialects to one registry repository, repull every manifest by\ndigest, and verify the complete dialect set. Only the exact selected\ndialects are written. Packaging and dry-run remain offline.\n\nText or JSON result goes to standard output. Diagnostics go to standard\nerror.\n\nExit status:\n  0  publication or dry-run verification completed\n  2  the command was used incorrectly\n  3  publication could not be verified",
      "examples": "rootform publish dialects ./oci --to r.ex/ext\n  rootform publish dialects ./oci --to r.ex/ext --dry-run\n  rootform publish dialects ./oci --to r.ex/ext --dry-run --format json",
      "flags": [
        {
          "name": "dry-run",
          "type": "bool",
          "default": "false",
          "usage": "report verified publication plan without network access",
          "no_option_default": "true"
        },
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform publish dialects",
          "no_option_default": "true"
        },
        {
          "name": "to",
          "type": "string",
          "default": "",
          "usage": "publish to tagless OCI `repository`"
        }
      ]
    },
    {
      "path": "rootform publish policy-packs",
      "usage": "rootform publish policy-packs \u003clayout\u003e [flags]",
      "summary": "Publish a verified Policy Pack registry layout",
      "description": "Validate an existing local Policy Pack registry layout, publish every pack\nto one registry repository, and repull each manifest by digest. Policy\nPacks are published exactly as selected. Dry-run remains offline.\n\nText or JSON result goes to standard output. Diagnostics go to standard\nerror.\n\nExit status:\n  0  publication or dry-run verification completed\n  2  the command was used incorrectly\n  3  publication could not be verified",
      "examples": "rootform publish policy-packs ./oci --to registry.example/acme/policies\n  rootform publish policy-packs ./oci --to localhost:5000/acme/policies\n  rootform publish policy-packs ./oci --to registry.example/acme/policies \\\n    --dry-run --format json",
      "flags": [
        {
          "name": "dry-run",
          "type": "bool",
          "default": "false",
          "usage": "report verified publication plan without network access",
          "no_option_default": "true"
        },
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform publish policy-packs",
          "no_option_default": "true"
        },
        {
          "name": "to",
          "type": "string",
          "default": "",
          "usage": "publish to tagless OCI `repository`"
        }
      ]
    },
    {
      "path": "rootform run",
      "usage": "rootform run [input] [flags]",
      "summary": "Explore an architecture locally",
      "description": "Build or load an architecture and serve its interactive interface\nlocally.\n\nThe input can be an infrastructure directory or a Rootform architecture\nfile. With no input, run reads the current directory. --plan reads a plan\nin JSON format instead; use - to read it from standard input. Directory\ninput never downloads, acquires, or prompts: selected external content\nmust already be available locally, or be prepared explicitly with\nrootform init or rootform vendor.\n\nThe local address goes to standard output. Diagnostics go to standard\nerror.\n\nExit status:\n  0  the local interface stopped cleanly\n  1  the local interface could not start\n  2  the command was used incorrectly",
      "examples": "rootform run\n  rootform run ./infra\n  rootform run architecture.json --no-browser\n  rootform run --plan tfplan.json",
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform run",
          "no_option_default": "true"
        },
        {
          "name": "locked",
          "type": "bool",
          "default": "false",
          "usage": "require an existing valid rootform.lock",
          "no_option_default": "true"
        },
        {
          "name": "no-browser",
          "type": "bool",
          "default": "false",
          "usage": "do not open the browser automatically",
          "no_option_default": "true"
        },
        {
          "name": "no-watch",
          "type": "bool",
          "default": "false",
          "usage": "build once instead of rebuilding when a file changes",
          "no_option_default": "true"
        },
        {
          "name": "plan",
          "type": "file",
          "default": "",
          "usage": "read JSON plan; - reads standard input"
        },
        {
          "name": "port",
          "type": "int",
          "default": "21717",
          "usage": "serve on local `port`; 0 picks a free one"
        }
      ]
    },
    {
      "path": "rootform show",
      "usage": "rootform show \u003cname\u003e [flags]",
      "summary": "Show a Rootform definition",
      "description": "Show one dialect, the rf vocabulary, or one declaration they\nmake.\n\nA bare owner name such as google or rf shows that owner and every\ndeclaration it makes. A qualified \u003cowner\u003e.\u003ckind\u003e.\u003cname\u003e shows one\ndeclaration, where kind is concept, context, relation, or rule. A\nbare declaration name is accepted when it resolves unambiguously.\n\nThe text or JSON definition goes to standard output. Diagnostics go\nto standard error.\n\nExit status:\n  0  the definition was shown\n  1  the named definition was not found\n  2  the command was used incorrectly\n  3  no single definition could be selected",
      "examples": "rootform show google\n  rootform show google.rule.cloud-sql-instance\n  rootform show google.relation.runs-as\n  rootform show rf.concept.virtual-network\n  rootform show google -o json",
      "subcommands": [
        "rootform show policy",
        "rootform show policy-pack"
      ],
      "flags": [
        {
          "name": "format",
          "shorthand": "o",
          "type": "string",
          "default": "",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform show",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform show policy",
      "usage": "rootform show policy \u003cidentifier\u003e [flags]",
      "summary": "Show a policy definition",
      "description": "Show a policy's target, assertion, message, Policy Pack, and source\nlocation.\n\nThe project must select the owning Policy Pack, or --policy-pack can\nsupply a local root. Use \u003cpolicy-pack\u003e.policy.\u003cname\u003e, or a bare name\nwhen it resolves unambiguously.\n\nUse \"rootform explain policy\" to understand why a policy produced a\nresult for an architecture element.\n\nThe text or JSON definition goes to standard output. Diagnostics go\nto standard error.\n\nExit status:\n  0  the definition was shown\n  1  the named definition was not found\n  2  the command was used incorrectly\n  3  no single definition could be selected",
      "examples": "rootform show policy baseline.policy.cluster-network-context\n  rootform show policy cluster-network-context\n  rootform show policy cluster-network-context --policy-pack ./policies\n  rootform show policy baseline.policy.cluster-network-context -o json",
      "flags": [
        {
          "name": "format",
          "shorthand": "o",
          "type": "string",
          "default": "",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform show policy",
          "no_option_default": "true"
        },
        {
          "name": "policy-pack",
          "type": "stringArray",
          "default": "[]",
          "usage": "select local Policy Pack `directory`; repeatable"
        }
      ]
    },
    {
      "path": "rootform show policy-pack",
      "usage": "rootform show policy-pack \u003cname\u003e [flags]",
      "summary": "Show a Policy Pack",
      "description": "Show a selected or local Policy Pack, including its version, the\npolicies it declares, its content identity, and its source location.\n\nThe name selects one loaded Policy Pack. With --policy-pack, only the\nsupplied local authoring roots are read.\n\nThe text or JSON definition goes to standard output. Diagnostics go\nto standard error.\n\nExit status:\n  0  the definition was shown\n  1  the named definition was not found\n  2  the command was used incorrectly\n  3  no single definition could be selected",
      "examples": "rootform show policy-pack baseline\n  rootform show policy-pack baseline --policy-pack ./policies\n  rootform show policy-pack baseline -o json",
      "flags": [
        {
          "name": "format",
          "shorthand": "o",
          "type": "string",
          "default": "",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform show policy-pack",
          "no_option_default": "true"
        },
        {
          "name": "policy-pack",
          "type": "stringArray",
          "default": "[]",
          "usage": "select local Policy Pack `directory`; repeatable"
        }
      ]
    },
    {
      "path": "rootform test",
      "usage": "rootform test [directory] [flags]",
      "summary": "Test dialect fixtures",
      "description": "Build dialect fixtures and compare their architectures with the\nexpected results.\n\nWith no directory, test reads the current directory. Text or JSON results\ngo to standard output. Diagnostics go to standard error.\n\nExit status:\n  0  every selected fixture passed\n  1  at least one fixture differed or encountered an error\n  2  the command was used incorrectly\n  3  the run could not start or be reported, or no fixtures matched",
      "examples": "rootform test\n  rootform test ./fixtures\n  rootform test ./fixtures --run cloud-sql\n  rootform test ./fixtures --format json",
      "flags": [
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform test",
          "no_option_default": "true"
        },
        {
          "name": "run",
          "type": "string",
          "default": "",
          "usage": "run only the cases whose `name` contains this text"
        }
      ]
    },
    {
      "path": "rootform validate",
      "usage": "rootform validate \u003cobject\u003e [flags]",
      "summary": "Validate a Rootform object",
      "description": "Validate an architecture or Rootform definition and report any problems\nfound.\n\nValidation checks definitions; it does not evaluate policies. Use\n\"rootform check\" for that.",
      "examples": "rootform validate architecture ./infra\n  rootform validate dialects ./dialects\n  rootform validate rule google.rule.cloud-sql-instance",
      "subcommands": [
        "rootform validate architecture",
        "rootform validate concept",
        "rootform validate context",
        "rootform validate dialects",
        "rootform validate policy",
        "rootform validate relation",
        "rootform validate rule"
      ],
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform validate",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform validate architecture",
      "usage": "rootform validate architecture [input] [flags]",
      "summary": "Validate an architecture",
      "description": "Build or load an architecture and check that it is structurally valid\nand internally consistent.\n\nThe input can be an infrastructure directory, a Rootform architecture\nfile, or - for an architecture file on standard input. With no input,\nvalidation reads the current directory.\n\nThe text or JSON result goes to standard output. Diagnostics go to\nstandard error.\n\nExit status:\n  0  the architecture is valid\n  1  the architecture is not valid\n  2  the command was used incorrectly\n  3  the architecture could not be validated",
      "examples": "rootform validate architecture\n  rootform validate architecture architecture.json\n  cat architecture.json | rootform validate architecture -",
      "flags": [
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform validate architecture",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform validate concept",
      "usage": "rootform validate concept \u003cidentifier\u003e [flags]",
      "summary": "Validate a concept definition",
      "description": "Validate a concept in the context of its dialect.\n\nUse \u003cowner\u003e.\u003ckind\u003e.\u003cname\u003e, or a bare name when it resolves unambiguously.\nThe text or JSON result goes to standard output. Diagnostics go to\nstandard error.\n\nExit status:\n  0  the definition is valid\n  1  the definition is not valid\n  2  the command was used incorrectly\n  3  the definition could not be validated",
      "examples": "rootform validate concept rf.concept.virtual-network\n  rootform validate concept virtual-network\n  rootform validate concept rf.concept.virtual-network --format json",
      "flags": [
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform validate concept",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform validate context",
      "usage": "rootform validate context \u003cidentifier\u003e [flags]",
      "summary": "Validate a context dimension",
      "description": "Validate a context dimension in the context of its dialect.\n\nUse \u003cowner\u003e.\u003ckind\u003e.\u003cname\u003e, or a bare name when it resolves unambiguously.\nThe text or JSON result goes to standard output. Diagnostics go to\nstandard error.\n\nExit status:\n  0  the definition is valid\n  1  the definition is not valid\n  2  the command was used incorrectly\n  3  the definition could not be validated",
      "examples": "rootform validate context rf.context.network\n  rootform validate context network\n  rootform validate context rf.context.network --format json",
      "flags": [
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform validate context",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform validate dialects",
      "usage": "rootform validate dialects [directory] [flags]",
      "summary": "Validate dialect definitions",
      "description": "Compile and validate a dialect, including its concepts, rules,\nand references.\n\nWith no directory, validation reads the current directory. The text or\nJSON result goes to standard output. Diagnostics go to standard error.\n\nExit status:\n  0  every dialect is valid\n  1  at least one dialect is not valid\n  2  the command was used incorrectly\n  3  no dialect result could be decided",
      "examples": "rootform validate dialects\n  rootform validate dialects ./dialects\n  rootform validate dialects ./dialects --format json",
      "flags": [
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform validate dialects",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform validate policy",
      "usage": "rootform validate policy \u003cidentifier\u003e [flags]",
      "summary": "Validate a policy definition",
      "description": "Validate a policy definition in its selected Policy Pack.\n\nThe project must select the Policy Pack that owns the policy.\nUse \u003cpolicy-pack\u003e.policy.\u003cname\u003e, or a bare name when it resolves unambiguously.\nThe text or JSON result goes to standard output. Diagnostics go to\nstandard error.\n\nExit status:\n  0  the definition is valid\n  1  the definition is not valid\n  2  the command was used incorrectly\n  3  the definition could not be validated",
      "examples": "rootform validate policy baseline.policy.cluster-network-context\n  rootform validate policy cluster-network-context\n  rootform validate policy baseline.policy.cluster-network-context --format json",
      "flags": [
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform validate policy",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform validate relation",
      "usage": "rootform validate relation \u003cidentifier\u003e [flags]",
      "summary": "Validate a relation predicate",
      "description": "Validate a relation and every compiled producer reference.\n\nUse \u003cowner\u003e.\u003ckind\u003e.\u003cname\u003e, or a bare name when it resolves unambiguously.\nThe text or JSON result goes to standard output. Diagnostics go to\nstandard error.\n\nExit status:\n  0  the definition is valid\n  1  the definition is not valid\n  2  the command was used incorrectly\n  3  the definition could not be validated",
      "examples": "rootform validate relation google.relation.runs-as\n  rootform validate relation runs-as\n  rootform validate relation google.relation.runs-as --format json",
      "flags": [
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform validate relation",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform validate rule",
      "usage": "rootform validate rule \u003cidentifier\u003e [flags]",
      "summary": "Validate a rule definition",
      "description": "Validate a rule and its references in the context of its dialect.\n\nUse \u003cowner\u003e.\u003ckind\u003e.\u003cname\u003e, or a bare name when it resolves unambiguously.\nThe text or JSON result goes to standard output. Diagnostics go to\nstandard error.\n\nExit status:\n  0  the definition is valid\n  1  the definition is not valid\n  2  the command was used incorrectly\n  3  the definition could not be validated",
      "examples": "rootform validate rule google.rule.cloud-sql-instance\n  rootform validate rule cloud-sql-instance\n  rootform validate rule google.rule.cloud-sql-instance --format json",
      "flags": [
        {
          "name": "format",
          "type": "string",
          "default": "text",
          "usage": "output `format`: text or json"
        },
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform validate rule",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform vendor",
      "usage": "rootform vendor \u003cobject\u003e [flags]",
      "summary": "Vendor selected non-embedded content",
      "description": "Materialize the project's exact non-embedded selections: dialects\nand Policy Pack sources, with their licenses and notices.",
      "examples": "rootform vendor dialects\n  rootform vendor policy-packs",
      "subcommands": [
        "rootform vendor dialects",
        "rootform vendor policy-packs"
      ],
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform vendor",
          "no_option_default": "true"
        }
      ]
    },
    {
      "path": "rootform vendor dialects",
      "usage": "rootform vendor dialects [flags]",
      "summary": "Vendor selected dialects",
      "description": "Materialize the exact dialect selection from rootform.lock: external\ndialects from remote or local sources, with their licenses and notices.\nPolicy Packs have their own vendoring destination and are never\nmaterialized here. Supplied dialects, the RF vocabulary, and derived\ncaches are never materialized. No version is resolved and rootform.lock\nis never changed.\n\nWith no --to flag, vendor writes ./.rootform/dialects. Consuming\ncommands launched from that project root use that directory as the\nexclusive source for these selections when present.\n\nCopied names and versions go to standard output. Diagnostics go to\nstandard error.\n\nExit status:\n  0  every selected dialect was copied\n  2  the command was used incorrectly\n  3  no complete vendored set was written",
      "examples": "rootform init ./infra --no-input\n  rootform vendor dialects\n  rootform vendor dialects --to ./offline/dialects",
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform vendor dialects",
          "no_option_default": "true"
        },
        {
          "name": "offline",
          "type": "bool",
          "default": "false",
          "usage": "use only exact local or cached dialects",
          "no_option_default": "true"
        },
        {
          "name": "to",
          "type": "string",
          "default": "",
          "usage": "copy into `directory`; ./.rootform/dialects by default"
        }
      ]
    },
    {
      "path": "rootform vendor policy-packs",
      "usage": "rootform vendor policy-packs [flags]",
      "summary": "Vendor selected Policy Packs",
      "description": "Materialize the exact rootform.lock Policy Pack selection from the\nlocal store, the verified content cache, or each pinned registry\nrepository when network access is explicitly allowed. Offline mode\npermits only verified local entries. No version is resolved and\nrootform.lock is never changed.\n\nWith no --to flag, vendor writes ./.rootform/policy-packs. Commands\nusing the project's Policy Pack selection use that directory\nexclusively when present. An explicit local --policy-pack selection\nreplaces the project selection for that invocation.\n\nCopied names and versions go to standard output. Diagnostics go to\nstandard error.\n\nExit status:\n  0  every selected Policy Pack was copied\n  2  the command was used incorrectly\n  3  no complete vendored set was written",
      "examples": "rootform vendor policy-packs\n  rootform vendor policy-packs --to ./offline/policy-packs\n  rootform package policy-packs ./policies --to ./artifacts/policies",
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform vendor policy-packs",
          "no_option_default": "true"
        },
        {
          "name": "offline",
          "type": "bool",
          "default": "false",
          "usage": "use only exact local or cached Policy Packs",
          "no_option_default": "true"
        },
        {
          "name": "to",
          "type": "string",
          "default": "",
          "usage": "copy into `directory`; ./.rootform/policy-packs by default"
        }
      ]
    },
    {
      "path": "rootform version",
      "usage": "rootform version [flags]",
      "summary": "Show the Rootform version",
      "description": "Print the Rootform version to standard output. Diagnostics go to\nstandard error.\n\nExit status:\n  0  the version was printed\n  2  the command was used incorrectly",
      "examples": "rootform version\n  rootform --version\n  rootform version \u003e rootform-version.txt",
      "flags": [
        {
          "name": "help",
          "shorthand": "h",
          "type": "bool",
          "default": "false",
          "usage": "show how to use rootform version",
          "no_option_default": "true"
        }
      ]
    }
  ]
}
