# Maintained directly from pinned provider evidence.
rule "hdinsight-hadoop-cluster" {
  match {
    type = "azurerm_hdinsight_hadoop_cluster"
  }

  as = concept.analytics-cluster

  context {
    as       = context.ownership
    to       = concept.resource-group
    via      = source.resource_group_name
    on_null  = "absent"
    on_empty = "absent"

    match {
      by       = target.name
      strategy = "exact"
    }

    # Shared resource-group instances can be provisioned by a separate configuration.
    external = "allow"
  }
}

rule "hdinsight-hbase-cluster" {
  match {
    type = "azurerm_hdinsight_hbase_cluster"
  }

  as = concept.analytics-cluster

  context {
    as       = context.ownership
    to       = concept.resource-group
    via      = source.resource_group_name
    on_null  = "absent"
    on_empty = "absent"

    match {
      by       = target.name
      strategy = "exact"
    }

    # Shared resource-group instances can be provisioned by a separate configuration.
    external = "allow"
  }
}

rule "hdinsight-interactive-query-cluster" {
  match {
    type = "azurerm_hdinsight_interactive_query_cluster"
  }

  as = concept.analytics-cluster

  context {
    as       = context.ownership
    to       = concept.resource-group
    via      = source.resource_group_name
    on_null  = "absent"
    on_empty = "absent"

    match {
      by       = target.name
      strategy = "exact"
    }

    # Shared resource-group instances can be provisioned by a separate configuration.
    external = "allow"
  }
}

rule "hdinsight-kafka-cluster" {
  match {
    type = "azurerm_hdinsight_kafka_cluster"
  }

  as = concept.analytics-cluster

  context {
    as       = context.ownership
    to       = concept.resource-group
    via      = source.resource_group_name
    on_null  = "absent"
    on_empty = "absent"

    match {
      by       = target.name
      strategy = "exact"
    }

    # Shared resource-group instances can be provisioned by a separate configuration.
    external = "allow"
  }
}

rule "hdinsight-spark-cluster" {
  match {
    type = "azurerm_hdinsight_spark_cluster"
  }

  as = concept.analytics-cluster

  context {
    as       = context.ownership
    to       = concept.resource-group
    via      = source.resource_group_name
    on_null  = "absent"
    on_empty = "absent"

    match {
      by       = target.name
      strategy = "exact"
    }

    # Shared resource-group instances can be provisioned by a separate configuration.
    external = "allow"
  }
}
