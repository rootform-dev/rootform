{
  "$comment": "Generated by `go run ./internal/language/schema/cmd -write`; DO NOT EDIT.",
  "$defs": {
    "boolean_binary": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "binary"
        },
        "left": {
          "$ref": "#/$defs/boolean_expression"
        },
        "operator": {
          "enum": [
            "\u0026\u0026",
            "||",
            "==",
            "!="
          ]
        },
        "range": {
          "$ref": "#/$defs/range"
        },
        "right": {
          "$ref": "#/$defs/boolean_expression"
        }
      },
      "required": [
        "kind",
        "left",
        "operator",
        "range",
        "right"
      ],
      "type": "object"
    },
    "boolean_expression": {
      "oneOf": [
        {
          "$ref": "#/$defs/boolean_literal"
        },
        {
          "$ref": "#/$defs/unary_expression"
        },
        {
          "$ref": "#/$defs/boolean_binary"
        },
        {
          "$ref": "#/$defs/number_binary"
        },
        {
          "$ref": "#/$defs/exists_expression"
        }
      ]
    },
    "boolean_literal": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "literal"
        },
        "range": {
          "$ref": "#/$defs/range"
        },
        "value": {
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "bool"
            },
            "value": {
              "type": "boolean"
            }
          },
          "required": [
            "kind",
            "value"
          ],
          "type": "object"
        }
      },
      "required": [
        "kind",
        "range",
        "value"
      ],
      "type": "object"
    },
    "concept_ref": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "$ref": "#/$defs/qualified_id"
        },
        "range": {
          "$ref": "#/$defs/range"
        }
      },
      "required": [
        "id",
        "range"
      ],
      "type": "object"
    },
    "context_ref": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "$ref": "#/$defs/qualified_id"
        },
        "range": {
          "$ref": "#/$defs/range"
        }
      },
      "required": [
        "id",
        "range"
      ],
      "type": "object"
    },
    "contexts_query": {
      "additionalProperties": false,
      "properties": {
        "dimension": {
          "$ref": "#/$defs/context_ref"
        },
        "kind": {
          "const": "contexts"
        },
        "range": {
          "$ref": "#/$defs/range"
        },
        "target": {
          "$ref": "#/$defs/concept_ref"
        }
      },
      "required": [
        "dimension",
        "kind",
        "range",
        "target"
      ],
      "type": "object"
    },
    "contributions_query": {
      "additionalProperties": false,
      "properties": {
        "contributor": {
          "$ref": "#/$defs/concept_ref"
        },
        "kind": {
          "const": "contributions"
        },
        "range": {
          "$ref": "#/$defs/range"
        }
      },
      "required": [
        "contributor",
        "kind",
        "range"
      ],
      "type": "object"
    },
    "digest": {
      "pattern": "^sha256:[0-9a-f]{64}(?![\\s\\S])",
      "type": "string"
    },
    "exists_expression": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "exists"
        },
        "query": {
          "$ref": "#/$defs/query"
        },
        "range": {
          "$ref": "#/$defs/range"
        }
      },
      "required": [
        "kind",
        "query",
        "range"
      ],
      "type": "object"
    },
    "identifier": {
      "maxLength": 64,
      "pattern": "^[a-z][a-z0-9]*(?:-[a-z0-9]+)*(?![\\s\\S])",
      "type": "string"
    },
    "length_expression": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "length"
        },
        "query": {
          "$ref": "#/$defs/query"
        },
        "range": {
          "$ref": "#/$defs/range"
        }
      },
      "required": [
        "kind",
        "query",
        "range"
      ],
      "type": "object"
    },
    "number_binary": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "binary"
        },
        "left": {
          "$ref": "#/$defs/number_expression"
        },
        "operator": {
          "enum": [
            "==",
            "!=",
            "\u003c",
            "\u003c=",
            "\u003e",
            "\u003e="
          ]
        },
        "range": {
          "$ref": "#/$defs/range"
        },
        "right": {
          "$ref": "#/$defs/number_expression"
        }
      },
      "required": [
        "kind",
        "left",
        "operator",
        "range",
        "right"
      ],
      "type": "object"
    },
    "number_expression": {
      "oneOf": [
        {
          "$ref": "#/$defs/number_literal"
        },
        {
          "$ref": "#/$defs/length_expression"
        }
      ]
    },
    "number_literal": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "literal"
        },
        "range": {
          "$ref": "#/$defs/range"
        },
        "value": {
          "additionalProperties": false,
          "properties": {
            "kind": {
              "const": "number"
            },
            "value": {
              "maximum": 9223372036854775807,
              "minimum": -9223372036854775808,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "value"
          ],
          "type": "object"
        }
      },
      "required": [
        "kind",
        "range",
        "value"
      ],
      "type": "object"
    },
    "policy": {
      "additionalProperties": false,
      "properties": {
        "assert": {
          "$ref": "#/$defs/boolean_expression"
        },
        "id": {
          "$ref": "#/$defs/qualified_id"
        },
        "message": {
          "maxLength": 1024,
          "minLength": 1,
          "type": "string"
        },
        "name": {
          "$ref": "#/$defs/identifier"
        },
        "pack": {
          "$ref": "#/$defs/identifier"
        },
        "range": {
          "$ref": "#/$defs/range"
        },
        "target": {
          "$ref": "#/$defs/concept_ref"
        }
      },
      "required": [
        "assert",
        "id",
        "message",
        "name",
        "pack",
        "range",
        "target"
      ],
      "type": "object"
    },
    "policy_pack": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "$ref": "#/$defs/identifier"
        },
        "range": {
          "$ref": "#/$defs/range"
        },
        "version": {
          "$ref": "#/$defs/version"
        }
      },
      "required": [
        "name",
        "range",
        "version"
      ],
      "type": "object"
    },
    "position": {
      "additionalProperties": false,
      "properties": {
        "byte_offset": {
          "maximum": 9223372036854775807,
          "minimum": 0,
          "type": "integer"
        },
        "column": {
          "maximum": 9223372036854775807,
          "minimum": 0,
          "type": "integer"
        },
        "line": {
          "maximum": 9223372036854775807,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "byte_offset",
        "column",
        "line"
      ],
      "type": "object"
    },
    "qualified_id": {
      "allOf": [
        {
          "pattern": "^[^/]{1,64}/[^/]{1,64}(?![\\s\\S])"
        }
      ],
      "pattern": "^[a-z][a-z0-9]*(?:-[a-z0-9]+)*/[a-z][a-z0-9]*(?:-[a-z0-9]+)*(?![\\s\\S])",
      "type": "string"
    },
    "query": {
      "oneOf": [
        {
          "$ref": "#/$defs/contexts_query"
        },
        {
          "$ref": "#/$defs/relations_query"
        },
        {
          "$ref": "#/$defs/contributions_query"
        }
      ]
    },
    "range": {
      "additionalProperties": false,
      "properties": {
        "end": {
          "$ref": "#/$defs/position"
        },
        "path": {
          "description": "Empty only for a zero range; otherwise a package-relative source path. The wire decoder rejects absolute paths, drive-qualified paths, and parent traversal.",
          "maxLength": 4096,
          "type": "string"
        },
        "start": {
          "$ref": "#/$defs/position"
        }
      },
      "required": [
        "end",
        "path",
        "start"
      ],
      "type": "object"
    },
    "relation_ref": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "$ref": "#/$defs/qualified_id"
        },
        "range": {
          "$ref": "#/$defs/range"
        }
      },
      "required": [
        "id",
        "range"
      ],
      "type": "object"
    },
    "relations_query": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "relations"
        },
        "predicate": {
          "$ref": "#/$defs/relation_ref"
        },
        "range": {
          "$ref": "#/$defs/range"
        },
        "target": {
          "$ref": "#/$defs/concept_ref"
        }
      },
      "required": [
        "kind",
        "predicate",
        "range",
        "target"
      ],
      "type": "object"
    },
    "semantic_pin": {
      "additionalProperties": false,
      "properties": {
        "id": {
          "$ref": "#/$defs/identifier"
        },
        "semantic_digest": {
          "$ref": "#/$defs/digest"
        },
        "version": {
          "$ref": "#/$defs/version"
        }
      },
      "required": [
        "id",
        "semantic_digest",
        "version"
      ],
      "type": "object"
    },
    "unary_expression": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "unary"
        },
        "operand": {
          "$ref": "#/$defs/boolean_expression"
        },
        "operator": {
          "const": "!"
        },
        "range": {
          "$ref": "#/$defs/range"
        }
      },
      "required": [
        "kind",
        "operand",
        "operator",
        "range"
      ],
      "type": "object"
    },
    "version": {
      "maxLength": 4096,
      "pattern": "^(?:0|[1-9][0-9]*)\\.(?:0|[1-9][0-9]*)\\.(?:0|[1-9][0-9]*)(?![\\s\\S])",
      "type": "string"
    }
  },
  "$id": "https://rootform.dev/schemas/compiled-policy-pack.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Evaluation-ready Policy Pack with exact historical semantic pins; no producer Dialect sources are required. Schema validation checks structure, not digest integrity or semantic linking. The decoder additionally verifies content_digest and compiled_digest, owner visibility, policy identity, unique IDs, canonical collection order, UTF-8 and duplicate JSON keys. String limits here count Unicode characters; the decoder enforces UTF-8 byte limits (4096 per string, 1024 per message, 4 MiB total). It also enforces 16 MiB serialized size, expression depth 64, 1024 expression nodes per Policy and 65536 per pack, JSON depth 80 and 2097152 JSON values. JSON Schema integers are mathematical values; the wire decoder requires int64 integer tokens, and positions must fit the host int. Definition ranges are retained for explanations and excluded from both digests.",
  "properties": {
    "compiled_digest": {
      "$ref": "#/$defs/digest",
      "description": "Canonical linked artifact digest, including content_digest and exact semantic pins; compiled_digest itself and all definition ranges excluded."
    },
    "content_digest": {
      "$ref": "#/$defs/digest",
      "description": "Canonical source Policy Pack digest: metadata, requirements reconstructed from pin IDs and versions, and policies; definition ranges excluded."
    },
    "format_version": {
      "const": "v1"
    },
    "policies": {
      "items": {
        "$ref": "#/$defs/policy"
      },
      "maxItems": 1024,
      "type": "array",
      "uniqueItems": true
    },
    "policy_pack": {
      "$ref": "#/$defs/policy_pack"
    },
    "semantics": {
      "items": {
        "$ref": "#/$defs/semantic_pin"
      },
      "maxItems": 1024,
      "type": "array",
      "uniqueItems": true
    }
  },
  "required": [
    "compiled_digest",
    "content_digest",
    "format_version",
    "policies",
    "policy_pack",
    "semantics"
  ],
  "title": "Rootform compiled Policy Pack",
  "type": "object"
}
