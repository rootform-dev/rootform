{
  "format_version": "1",
  "scenarios": [
    {
      "id": "kubernetes-showcase-semantics",
      "fixture": "fixtures/slice/kubernetes",
      "required_rules": [
        "kubernetes.rule.kubernetes-deployment",
        "kubernetes.rule.kubernetes-stateful-set",
        "kubernetes.rule.kubernetes-network-policy",
        "kubernetes.rule.kubernetes-namespace"
      ],
      "minimum": {
        "representations": 22,
        "relations": 2,
        "contexts": 33,
        "contributions": 0
      }
    },
    {
      "id": "kubernetes-aks-provider-host",
      "fixture": "fixtures/slice/aks-kubernetes",
      "required_rules": [
        "azure.rule.aks-cluster",
        "kubernetes.rule.kubernetes-namespace"
      ],
      "required_facts": [
        {
          "collection": "contexts",
          "from": "representation:1:root:resource:kubernetes_namespace_v1.apps",
          "to": "representation:1:root:resource:azurerm_kubernetes_cluster.runtime",
          "dimension": "rf.context.runtime",
          "rule": "kubernetes.rule.kubernetes-namespace"
        },
        {
          "collection": "contexts",
          "from": "representation:1:root:resource:kubernetes_namespace_v1.operations",
          "to": "representation:1:root:resource:azurerm_kubernetes_cluster.runtime",
          "dimension": "rf.context.runtime",
          "rule": "kubernetes.rule.kubernetes-namespace"
        }
      ],
      "maximum_facts": {
        "contexts": 3,
        "relations": 0,
        "contributions": 0
      },
      "limitations": [
        "A provider host assembled by a template, such as https://${azurerm_kubernetes_cluster.runtime.fqdn}:443, names no endpoint attribute, so the runtime cluster of kubernetes_namespace_v1.templated stays indeterminate(unavailable)."
      ]
    }
  ]
}
