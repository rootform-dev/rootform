{
  "format_version": "1",
  "toolchain": {
    "identity": "rootform 0.1.0",
    "binary_sha256": "sha256:52bd84bb2c6de982fe71c8af49c5b869d54b65c6d6a9592781ec46fe146f8058",
    "selection": "Supplied release set; no project lock or installation required.",
    "golden_command": "ROOTFORM_BIN=/absolute/path/to/rootform bun run update:goldens",
    "fixture_command": "ROOTFORM_HOME=${FRESH_ROOTFORM_HOME:?} $ROOTFORM_BIN test ./fixtures --run slice/kubernetes --format json",
    "golden_generator": "Generated by Rootform 0.1.0 with the semantic snapshot, qualified predicates, emission provenance, omissions, and emission-scoped diagnostics.",
    "verification_command": "ROOTFORM_BIN=$ROOTFORM_BIN bun scripts/verify.ts --rootform-version 0.1.0",
    "test_commands": [
      "$ROOTFORM_BIN test ./fixtures --format json"
    ]
  },
  "scenarios": [
    {
      "id": "kubernetes-showcase-semantics",
      "fixture": "fixtures/slice/kubernetes",
      "required_rules": [
        "kubernetes.rule.kubernetes-deployment",
        "kubernetes.rule.kubernetes-stateful-set",
        "kubernetes.rule.kubernetes-network-policy",
        "kubernetes.rule.kubernetes-namespace"
      ],
      "minimum": {
        "representations": 22,
        "relations": 2,
        "contexts": 33,
        "contributions": 0
      },
      "negative_assertions": [
        "literal_service_account has no runs-as relation",
        "unknown_service_account has no runs-as relation",
        "ambiguous_service_account has no runs-as relation",
        "literal network policy has no ownership context",
        "unknown network policy has no ownership context",
        "ambiguous network policy has no ownership context"
      ]
    }
  ]
}
